# RDN Benchmarks

Cross-implementation benchmark harness for RDN parsers and serializers.

## Running

```bash
./run.sh
```

## Fixtures

- `small.rdn` — Minimal JSON-compatible object
- `medium.rdn` — Array with RDN types (dates, sets)
- `large-mixed.rdn` — Complex document using all RDN types
